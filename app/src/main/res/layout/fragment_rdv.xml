<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F5F5F5"
    tools:context=".RdvFragment">

    <!-- Header : spinners -->
    <LinearLayout
        android:id="@+id/llHeader"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="12dp"
        app:layout_constraintTop_toTopOf="parent">

        <Spinner
            android:id="@+id/spinnerCentre"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_spinner"
            android:padding="8dp" />

        <Spinner
            android:id="@+id/spinnerStatut"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:background="@drawable/bg_spinner"
            android:padding="8dp" />

    </LinearLayout>

    <!-- Boutons : Rafraîchir / File / Flacons -->
    <LinearLayout
        android:id="@+id/llActions1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        app:layout_constraintTop_toBottomOf="@id/llHeader">

        <Button
            android:id="@+id/btnRafraichir"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginEnd="4dp"
            android:text="🔄 Rafraîchir"
            android:textColor="#2196F3"
            app:strokeColor="#2196F3" />

        <Button
            android:id="@+id/btnFile"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="4dp"
            android:layout_marginEnd="4dp"
            android:text="📋 File"
            android:textColor="#4CAF50"
            app:strokeColor="#4CAF50" />

        <Button
            android:id="@+id/btnFlacons"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="4dp"
            android:text="💊 Flacons"
            android:textColor="#FFC107"
            app:strokeColor="#FFC107" />

    </LinearLayout>

    <!-- Boutons : Démarrer / Terminer -->
    <LinearLayout
        android:id="@+id/llActions2"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="12dp"
        app:layout_constraintTop_toBottomOf="@id/llActions1">

        <Button
            android:id="@+id/btnDemarrer"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginEnd="4dp"
            android:text="▶ Démarrer"
            android:textColor="@android:color/white"
            android:backgroundTint="#4CAF50" />

        <Button
            android:id="@+id/btnTerminer"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="4dp"
            android:text="⏹ Terminer"
            android:textColor="@android:color/white"
            android:backgroundTint="#F44336" />

    </LinearLayout>

    <!-- Liste RDV -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvRdv"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:paddingHorizontal="12dp"
        android:clipToPadding="false"
        app:layout_constraintTop_toBottomOf="@id/llActions2"
        app:layout_constraintBottom_toBottomOf="parent"
        tools:listitem="@layout/item_rdv" />

</androidx.constraintlayout.widget.ConstraintLayout>