<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/cardCentre"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="8dp"
    app:cardCornerRadius="14dp"
    app:cardElevation="3dp"
    app:cardBackgroundColor="#FFFFFF">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="14dp">

        <!-- Ligne nom + statut -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="6dp">

            <TextView
                android:id="@+id/tvNomCentre"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:textStyle="bold"
                android:textSize="15sp"
                android:textColor="#222222" />

            <TextView
                android:id="@+id/tvStatutCentre"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="13sp"
                android:textStyle="bold" />
        </LinearLayout>

        <!-- Adresse -->
        <TextView
            android:id="@+id/tvAdresseCentre"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="13sp"
            android:textColor="#666666"
            android:drawableStart="@android:drawable/ic_dialog_map"
            android:drawablePadding="6dp"
            android:layout_marginBottom="4dp" />

        <!-- Téléphone -->
        <TextView
            android:id="@+id/tvTelCentre"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="12sp"
            android:textColor="#888888"
            android:layout_marginBottom="10dp" />

        <!-- Bouton détail (visible si actif) -->
        <Button
            android:id="@+id/btnDetailCentre"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:text="Voir les détails"
            android:textColor="#FFFFFF"
            android:textSize="13sp"
            android:backgroundTint="#F5A623"
            android:visibility="gone" />

    </LinearLayout>
</androidx.cardview.widget.CardView>