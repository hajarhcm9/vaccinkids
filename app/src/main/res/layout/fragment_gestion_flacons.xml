<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#FFF8F0"
    android:padding="16dp">

    <!-- Header -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="💉 Gestion des flacons"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="#333333"
        android:layout_marginBottom="4dp" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Suivi des doses utilisées et gaspillées"
        android:textSize="13sp"
        android:textColor="#888888"
        android:layout_marginBottom="16dp" />

    <!-- Bouton Ouvrir flacon -->
    <Button
        android:id="@+id/btnOuvrirFlacon"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:text="+ Ouvrir un nouveau flacon"
        android:textColor="#FFFFFF"
        android:backgroundTint="#F5A623"
        android:layout_marginBottom="16dp" />

    <!-- Message aucun flacon -->
    <TextView
        android:id="@+id/tvAucunFlacon"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Aucun flacon ouvert actuellement."
        android:textColor="#AAAAAA"
        android:gravity="center"
        android:padding="32dp"
        android:visibility="gone" />

    <!-- Liste flacons -->
    <ListView
        android:id="@+id/listViewFlacons"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:divider="@null"
        android:dividerHeight="8dp" />

</LinearLayout>