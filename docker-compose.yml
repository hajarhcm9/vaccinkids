services:
  redis:
    image: redis:7-alpine
    container_name: vaccinikids-redis
    restart: unless-stopped
    command: redis-server --appendonly yes
    ports:
      - '${REDIS_PORT:-6379}:6379'
    volumes:
      - redisdata:/data
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 10s
      timeout: 5s
      retries: 5

  postgres:
    image: postgres:15-alpine
    container_name: vaccinikids-db
    restart: unless-stopped
    ports:
      - '${DB_PORT:-5433}:5432'
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-vaccinikids_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-vaccinikids_password}
      POSTGRES_DB: ${POSTGRES_DB:-vaccinikids}
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER:-vaccinikids_user} -d ${POSTGRES_DB:-vaccinikids}']
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
    driver: local
  redisdata:
    driver: local
